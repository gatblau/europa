---
- include: tool.yml
  vars:
    tool_name: Maven
    folder_name: "{{version}}"
    zip_filename: "{{version}}-bin.zip"
    add_version_to_folder: "false"
    zip_has_folder: "true"
    has_bin_folder: "true"
