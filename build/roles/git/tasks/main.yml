---
- include: rpm.yml
  vars:
    rpm_app_name: GIT Wandisco yum repository
    rpm_filename: "{{ yum-repo }}"

- include: rpm.yml
  vars:
    rpm_app_name: GIT_PERL
    rpm_filename: "{{ perl-git-rmp}}"

- include: rpm.yml
  vars:
    rpm_app_name: GIT_SCM
    rpm_filename: "{{ git-rpm }}"