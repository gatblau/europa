---
- include: rpm.yml
  vars:
    description: GIT Wandisco yum repository
    rpmfile: "{{yumrepo}}"

- include: rpm.yml
  vars:
    description: GIT_PERL
    rpmfile: "{{perlgitrpm}}"

- include: rpm.yml
  vars:
    description: GIT_SCM
    rpmfile: "{{gitrpm}}"